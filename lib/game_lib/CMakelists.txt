add_library(
  game_lib
  game.h 
  game.cpp
  )

add_library(
  ship_lib
  ship.h
  ship.cpp
  )

add_library(
  strategy_lib
  strategy.h
  strategy.cpp
  )

add_library(
  field_lib
  field.h
  field.cpp
  )

target_link_libraries(game_lib PRIVATE ship_lib)
target_link_libraries(game_lib PRIVATE field_lib)
target_link_libraries(game_lib PRIVATE strategy_lib)